version: 0.2
phases:
install:
  commands:
    - npm install
build:
  commands:
    - echo test
    - npm run build
    - npm run cdk synth -- -o dist
artifacts:
  base-directory: dist
  files:
    - BucketStack.template.json